---
  - name: install spurtcommerce
    hosts: all
    become: yes
    vars:
      - mysql_user: root
      - mysql_password: root
    tasks:
      - name: installing nodejs
        command:
          cmd: sudo apt install nodejs
      

